# NodeJS
- Run js files using Node: node app.js
- Global Object
- `this` is {} empty object in node js

- Modules protect the variables and function by default from leaking without exporting we can't access them.
- 